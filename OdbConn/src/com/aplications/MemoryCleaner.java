package com.aplications;

import jdk.nashorn.internal.objects.NativeDebug;
import sun.misc.Cleaner;

/**
 * Created by Fahim on 7/9/2017.
 */
public class MemoryCleaner {
    public static void main(String[] args) {
  /*      NativeDebug buffer;
        Cleaner cleanerMethod = buffer.getClass().getMethod("cleaner");
        cleanerMethod.setAccessible(true);
        Object cleaner = cleanerMethod.invoke(buffer);
        Cleaner cleanMethod = cleaner.getClass().getMethod("clean");
        cleanMethod.setAccessible(true);
        cleanMethod.invoke(cleaner);

*/
        //Read more: http://www.java67.com/2014/01/how-to-fix-javalangoufofmemoryerror-direct-byte-buffer-java.html#ixzz4mJrAy7bg
    }
}
